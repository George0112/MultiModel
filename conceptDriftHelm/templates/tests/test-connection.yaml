apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "conceptDriftHelm.fullname" . }}-test-connection"
  labels:
{{ include "conceptDriftHelm.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "conceptDriftHelm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
